{
  "name": "chailyn-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"web,engine,api\" --prefix-colors \"cyan,yellow,green\" \"pnpm --filter web dev\" \"pnpm --filter pattern-engine dev\" \"cd services/api && set -a && . .env && set +a && .venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 --reload\"",
    "debug": "concurrently --names \"web,engine,api\" --prefix-colors \"cyan,yellow,green\" \"pnpm --filter web dev\" \"pnpm --filter pattern-engine dev\" \"cd services/api && DATABASE_URL=postgresql+asyncpg://chailyn:chailyn_dev@localhost:5432/chailyn PATTERN_ENGINE_URL=http://localhost:3001 .venv/bin/uvicorn main:app --port 8000 --reload --log-level debug\"",
    "build": "pnpm --filter web build && pnpm --filter pattern-engine build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
